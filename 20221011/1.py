#    напишите функцию multiplier(x, y, Type), 
#возвращающую результат умножения x на y

from decimal import *
from fraction import *

def multiplier(x,y,type):
	
	return x*y

#multiplier = lambda x, y, cls = float: cls(x) * cls(y)
